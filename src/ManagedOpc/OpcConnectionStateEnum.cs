using System;

namespace Autabee.Communication.ManagedOpc
{
    public enum OpcConnectionStatus
    {
        Unkown = 0,
        Disconnected = 1,
        Reconnecting = 2,
        Connected = 3,
        
    }
}
